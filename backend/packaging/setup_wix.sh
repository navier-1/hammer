# Works on powershell, mingw and msys shell

wix extension add WixToolset.UI.wixext
wix extension add WixToolset.Util.wixext

# or add -g ? This probably requires admin privileges

# Verify UI extension:
# wix extension list